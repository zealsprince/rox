//! Shared track-row UI: the per-column cell renderers, the column model
//! and header menus, and the drag payload for dragging tracks out of a list.

pub mod track_cells;
pub mod track_columns;
pub mod track_drag;
