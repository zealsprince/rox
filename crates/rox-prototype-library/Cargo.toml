[package]
name = "rox-prototype-library"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
# The store and projection under test graduated into rox-library.
rox-library = { path = "../rox-library" }
rusqlite = "0.34"
