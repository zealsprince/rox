[package]
name = "rox-prototype-playback"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
# The spike's engine graduated into rox-playback; this binary keeps driving it.
rox-playback = { path = "../rox-playback" }
