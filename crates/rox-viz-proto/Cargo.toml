[package]
name = "rox-viz-proto"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
gpui = { workspace = true }
# Same major as gpui's own image dependency, so Frame/RgbaImage are one type.
image = "0.25"
