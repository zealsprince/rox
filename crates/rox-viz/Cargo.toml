[package]
name = "rox-viz"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
# Signals and routes ride the workspace bundle's derived JSON Schema (ADR 22).
schemars = "1"

[dev-dependencies]
serde_json = "1"
